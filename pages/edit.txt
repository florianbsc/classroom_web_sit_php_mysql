<?php
if (isset($_GET['id'])) {
    $recipeId = $_GET['id'];

    // Récupérer les données existantes de la recette
    $existingRecipe = getRecipeById($recipeId, $db);

    if (!$existingRecipe) {
        // Gérer le cas où la recette n'est pas trouvée
        echo 'Recette non trouvée';
    } else {
        // Afficher le formulaire de modification avec les données existantes
        ?>
        <form method="post" action="./pages/process_update.php">
            <input type="hidden" name="recipe_id" value="<?php echo $recipeId; ?>">
            Titre: <input type="text" name="new_title" value="<?php echo $existingRecipe['title']; ?>"><br>
            Recette: <textarea name="new_description"><?php echo $existingRecipe['recipe']; ?></textarea><br>
            <input type="submit" value="Modifier">
        </form>
        <?php
    }
} else {
    // Gérer le cas où l'ID n'est pas spécifié
    echo 'ID de recette non spécifié';
}
?>
